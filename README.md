# Web Access Worker

Release **1.1.0**, compatibility protocol **1.1**, deployment kind **aper-web-access**. This independent, dependency-free Worker acquires bounded public static documents for a user-owned Aper installation. `src/worker.js` owns authentication, exact-origin browser access, target validation, acquisition, and the versioned HTTP contract. It performs no Cloudflare management operations and has no MCP routes, rendering, persistence, extraction, or telemetry. Wrangler and Vite+ are pinned development dependencies; `pnpm-lock.yaml` locks the toolchain.

## Install and connect

1. In Aper, open **Settings → Local Tools → Web Access Worker → Set up Worker**.
2. Choose **Copy secret**, then **Deploy to Cloudflare**. You need Cloudflare and GitHub or GitLab accounts. Cloudflare opens in a new tab and creates a copy of the template in your Git account.
3. In Cloudflare, select your account. Set the variable `APER_ALLOWED_ORIGIN` to the exact address shown in Aper, including the scheme and development port, with no trailing slash. Paste the copied secret into the **secret** named `APER_RUNTIME_SECRET`; keep it out of source files and ordinary variables.
4. Complete deployment, return to Aper, paste your Worker URL, and choose **Connect**. Aper checks the connection and fetches a test page from example.com. Your setup stays saved if you leave the tab or a check fails.

Use **Replace Worker** to connect a new deployment. Your existing connection stays in place until the new one passes its checks. **Disconnect** only removes the connection from Aper; delete the Worker or change its secret in Cloudflare.

The previously published template release **1.0.0** is available from [the public repository](https://github.com/ent3m/aper-web-access). The installer copies the repository's current source; it does not promise an immutable release selection. Your copy does not automatically receive Aper updates. The local **1.1.0** implementation adds relays; its publication and authenticated hosted relay verification are not established by the earlier 1.0 evidence.

For independent command-line deployment, run `vp install --frozen-lockfile`, configure the origin in your copy of `wrangler.jsonc`, run `vp exec wrangler secret put APER_RUNTIME_SECRET` and paste the generated secret at its prompt, then `vp exec wrangler deploy`. All Cloudflare authorization remains in your CLI/account. Do not pass secrets as command arguments. Local development uses a gitignored `.dev.vars` copied from the empty example.

## Compatibility contract

| Route            | Input                                                                               | Success                                                                                                                                |
| ---------------- | ----------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `GET /v1/info`   | Runtime bearer authentication and exact configured Origin                           | `kind`, `protocolVersion`, `buildId`, `capabilities: { staticFetch, browserRun, searchRelay }`                                         |
| `POST /v1/fetch` | Same authentication/origin; JSON containing only `{ "url": "https://example.com" }` | `requestedUrl`, `finalUrl`, `status`, `contentType`, complete decoded `body`, `byteCount`, ordered `redirects: [{ from, to, status }]` |

Protocol major 1 preserves these operations and meanings; additive minor fields may be ignored. Breaking changes require another major/route family. Build identity is independently versioned with the template release. Compatible older or user-modified deployments remain eligible; reported build and capabilities are compatibility observations, not source attestations or ongoing service-health guarantees. The hosted installer uses the public repository URL; no immutable source pin is claimed. A user's repository copy does not automatically track Aper releases. Review the offered release and your own changes before updating.

Errors are non-2xx JSON `{ "error": "code" }`; raw upstream bodies, exceptions, headers, and secrets are never logged or returned as errors. Codes distinguish origin/auth/configuration, invalid request/target, forbidden target, redirect limit, header/body limits, unsupported documents, encoding, deadline/cancellation, and acquisition failure. Browser CORS/CSP/network failures can be ambiguous. Preflight permits only the method for the route and Authorization/Content-Type; CORS is additional browser control, never authentication. Responses disable caching.

## Search relay

`POST /v1/search/{brave|exa|firecrawl|you}` shares static-fetch authentication, exact-origin CORS, request/body/header bounds and the total deadline. Its JSON contains only `{ body, credential }`: the existing supported provider search shape and a distinct search API key. `search-relay.js` validates exact required/optional keys and fixed defaults before constructing the allowlisted URL, POST method and provider auth header. Arbitrary URLs, methods, headers, cookies, scraping, synthesis and redirect following are unavailable. A runtime secret cannot be used as the provider key. No Cloudflare management authorization or additional binding/resource is required.

Relay success returns `{ status, body, quota, byteCount, contacts, redirects: [] }`. Upstream rejection preserves status and allowlisted quota headers with null body. Responses are bounded to 2 MB before JSON decoding; observed headers are bounded to 32 KB. Transport, malformed JSON and redirect errors are sanitized. Static and relay failures retain bounded contact/redirect evidence. Ordinary extraction and search normalization stay in Aper. Relay shape tests and the real Chromium boundary script in Aper cover these controls; authenticated live provider success is not asserted by deterministic fixtures.

## Acquisition constraints and rationale

Acquisition constructs a GET with fixed textual Accept and User-Agent; it forwards no incoming cookies, credentials, arbitrary headers, body, or browser session. Only HTTP(S) DNS names on default ports are accepted. IP literals (including public ones), single-label/local/reserved names, URL credentials, and unsafe schemes are rejected. Every manually followed redirect is validated again. Limits are 8,192 URL characters, 10 KB input, five redirects, 32 KB observed response headers, 2 MB acquired bytes, and a 15-second total deadline. Only complete HTTP 200 HTML/XHTML/plain text is accepted. Unsupported content, partial responses, oversize bodies, and decoding errors fail instead of silently truncating. Charset comes from HTTP or defaults to UTF-8; extraction and fidelity assessment belong in Aper.

`global_fetch_strictly_public` ensures public routing rather than zone-origin shortcuts. Workers exposes no trustworthy application-controlled DNS pinning here, so hostname checks and platform public egress are defense in depth, not a claim of complete DNS-rebinding protection. Header limits apply after platform header acquisition. There is no arbitrary proxy/tunnel interface, but anyone holding the runtime secret and sending the configured Origin can consume public-fetch quota; CORS does not constrain non-browser clients. Platform account limits apply, and the owner controls costs and secret rotation.

## Lifecycle and verification

Replacement stages an independently authenticated deployment and verifies it before switching local configuration. Failed or cancelled setup leaves the last usable connection and user-modified infrastructure intact. Disconnect removes Aper's local connection only; it cannot revoke the remote secret or delete the Worker. Perform deletion and remote rotation explicitly in Cloudflare. Captured Aper bindings retain endpoint and credential identity so configuration changes do not retarget them. Web and MCP must use separate deployments, runtime secrets, protocols, routes, and capability configuration.

Run `vp check --fix`, `vp test`, `vp build`, then `vp exec wrangler deploy --dry-run --outdir dist/deploy`. Tests exercise authentication, CORS policy, target/redirect restrictions, request construction, bounds, encoding, and sanitized failure. Aper's Web and Application Configuration tests cover compatibility and persistent replacement/cancellation/capture. From the adjacent Aper repository, `vp exec node scripts/verify-web-worker-browser.mjs` runs Chromium against a real local HTTP/CORS boundary with deterministic public-document acquisition. It does not prove Cloudflare hosting.

Live evidence supplied by the user on 2026-09-13: Cloudflare's hosted installer cloned `ent3m/aper-web-access` into `ent3m/aper-web-access-test`, installed the locked toolchain, built, and deployed successfully at 2026-09-12T22:22:31Z. The deployment used `APER_ALLOWED_ORIGIN=http://localhost:5173` and endpoint `https://aper-web-access-test.badmovie100.workers.dev`, Cloudflare version `c34b087c-69ea-4449-8629-256f2c219781`. Aper reported successful browser verification at `2026-09-12T22:26:00.140Z`, protocol `1.0`, build `1.0.0`. The implemented verifier requires missing/wrong-secret rejection, correct-secret authentication, compatible kind/capabilities, and a successful static test fetch. This is user-observed hosted evidence, not an independently repeated live test or a source attestation. Wrong-origin rejection and replacement/disconnect failure paths are additionally covered by the local browser and persistence suites. The deployment log reported default-enabled preview URLs and Wrangler CLI telemetry; those messages do not establish application telemetry.

Implementation references: [Cloudflare Deploy buttons](https://developers.cloudflare.com/workers/platform/deploy-buttons/), [Worker secrets](https://developers.cloudflare.com/workers/configuration/secrets/), [Wrangler configuration](https://developers.cloudflare.com/workers/wrangler/configuration/), and [Fetch routing](https://developers.cloudflare.com/workers/runtime-apis/fetch/).
